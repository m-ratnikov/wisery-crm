// Drizzle schema entry point. Intentionally empty: domain tables arrive with
// features (the first entity - e.g. signals - lands its own migration). The
// pg-boss `pgboss` schema is created by pg-boss itself, not modeled here.
export {};
