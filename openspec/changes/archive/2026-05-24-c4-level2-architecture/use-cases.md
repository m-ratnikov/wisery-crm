## Use cases

Skip - out of scope for this change. This slice is not user-facing: it decomposes the inside of the system boundary into C4 L2 containers and the protocols between them, introducing no new actors or system-boundary goals. The actors and the primary journey are already established by the archived L1 change (`system-context-c4-l1`); the L2 system-design's runtime flow stays consistent with that journey rather than re-deriving it here.
