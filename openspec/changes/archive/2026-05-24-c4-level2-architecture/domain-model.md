## Domain model

Skip - no entity shape changes in this change. This slice shapes the C4 L2 container view (the runnable units inside the system boundary and the protocols between them); it decides runnable units and their interactions, not entity shapes. The glossary, ERD, lifecycle, and domain events belong to later per-area domain-model changes, not to this L2 container slice.
