Out of scope for this change. Where-things-run is unchanged: the same single Node process (web + in-process pg-boss worker roles), Managed Postgres (app data + pg-boss tables), and the on-demand provider/browser, per ADR-0001/0002/0004. The engagement work adds pg-boss handlers inside the existing worker role, not new infrastructure, so no topology, secrets, or scaling change is decided here. The conceptual scaling hook stays in system-design.md (cross-cutting concerns).
