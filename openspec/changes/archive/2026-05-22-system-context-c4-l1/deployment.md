## Deployment

Skip - out of scope for this change. Where-things-run (deployment topology) is a separate slice; this change shapes only the C4 L1 system context. The runtime decision in force is ADR-0001 (background jobs in-process on managed Postgres); a future deployment change will draw the topology.
