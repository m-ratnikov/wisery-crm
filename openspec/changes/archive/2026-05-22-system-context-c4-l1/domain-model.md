## Domain model

Skip - out of scope for this change. This slice shapes the C4 L1 system context (the system boundary and the external systems it talks to) and decides no entity shapes. The ERD, entity lifecycle, and domain events belong to later per-area domain-model changes, not to this system-context slice.
