# Use-cases view: out of scope for this change

No new actor or system-boundary goal. This change refines an internal data-model vocabulary (the `Prospect.status` set), not who interacts with the system or what they can do.
