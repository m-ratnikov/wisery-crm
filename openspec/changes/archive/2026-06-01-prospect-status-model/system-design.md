# System-design view: out of scope for this change

No container, external-system, or topology change. This is a domain-model vocabulary refinement (the `Prospect.status` set and which facts are derived from relations); the runtime flow and component structure are unchanged.
